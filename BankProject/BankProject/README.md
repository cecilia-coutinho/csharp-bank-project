***VG-kriterier:***

*Skriva en reflektion/ett resonemang där du motiverar för hur du valt att bygga upp ditt program. Du ska alltså resonera kring den lösning du valt, vilka andra du övervägt och kritiskt granska ditt val och eventuellt motivera för bättre lösningar som du ser men inte gjort. Denna motivering ska finnas i din Readme-fil.*

- At first I started adding methods directly in the class program.cs, but after then I realized it would be better to add them within the respective classes, and create instances to call a method or access them from program.cs. At last, I decided not to implement the improvement just because of time constraints.
- I decided to declare the users data as a static variable to be easier to access
- I followed the requirements to fulfill the VG-kriterier
- I tried 3 methods to create a database as an exercise (SQL, .txt and spreasheet). In the end, I decided to keep the data stored in a spreadsheet and I used an API to write and read it (I installed an API called "Bytescout.Spreadsheet" in the NuGet Packages).
- I decided not to work with a pseudocode in this file only because we got the diagram from school and, besides, it would be necessary to comment the code directly.